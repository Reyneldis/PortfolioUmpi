---
import AnimatedFooter from './FooterItem';
---

<AnimatedFooter client:load />

<!-- <style>
	/* Efecto de hover suave para los enlaces */
	a {
		position: relative;
	}

	a::after {
		content: '';
		position: absolute;
		width: 0;
		height: 1px;
		bottom: -2px;
		left: 0;
		background-color: #f97316;
		transition: width 0.3s ease;
	}

	a:hover::after {
		width: 100%;
	}

	/* Efecto de hover para los iconos */
	/* svg {
		transition: transform 0.3s ease;
	}

	a:hover svg {
		transform: translateY(-2px);
	} */

	/* Animaciones para móvil */
	@media (max-width: 640px) {
		footer {
			animation: fadeInUp 0.8s ease-out;
		}

		@keyframes fadeInUp {
			from {
				opacity: 0;
				transform: translateY(20px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	}

	/* Ajustes para móvil */
	@media (max-width: 640px) {
		footer {
			padding: 2rem 1rem;
		}
	}
</style> -->
