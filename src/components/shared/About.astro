---
import { AboutItem } from './AboutItem';

const ABOUT = [
	{
		title: 'Sobre mí',
		description:
			'Soy un desarrollador web de Camagüey, Cuba. Apasionado por la tecnología y la innovación, me especializo en crear soluciones digitales modernas y eficientes que ayuden a las empresas a crecer en el mundo digital.',
	},
	{
		title: 'Educación',
		description:
			'Graduado en Ingeniería Informática en la Universidad Ignacio Agramonte Loynaz de Camagüey, con especialización en desarrollo web y tecnologías modernas. Constantemente actualizando mis conocimientos para ofrecer las mejores soluciones.',
	},
	{
		title: 'Experiencia',
		description:
			'He trabajado en diversos proyectos de desarrollo web, tanto en Cuba como internacionalmente, creando aplicaciones web modernas y responsivas que se adaptan a las necesidades de cada cliente.',
	},
];
---

<section class="flex flex-col gap-8">
	<div class="flex flex-col gap-8">
		<div class="flex flex-col gap-4">
			<!-- <h2 class="text-2xl font-bold text-gray-900 dark:text-white">Acerca de mí</h2> -->
			<p class="text-gray-600 dark:text-gray-400">
				Desarrollador web de Camagüey, Cuba, graduado en la Universidad Ignacio Agramonte Loynaz,
				especializado en crear experiencias digitales únicas.
			</p>
		</div>

		<div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
			{ABOUT.map((item, index) => <AboutItem {...item} index={index} client:load />)}
		</div>
	</div>
</section>

<style>
	/* Animaciones para móvil */
	@media (max-width: 640px) {
		section {
			animation: fadeInUp 0.8s ease-out;
		}

		@keyframes fadeInUp {
			from {
				opacity: 0;
				transform: translateY(20px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	}

	/* Efectos de hover */
	div {
		transition: transform 0.3s ease;
	}

	div:hover {
		transform: translateY(-4px);
	}

	/* Efecto de brillo en el título */
	h2 {
		position: relative;
		display: inline-block;
	}

	h2::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 0;
		height: 2px;
		background: linear-gradient(to right, #f97316, transparent);
		transition: width 0.3s ease;
	}

	h2:hover::after {
		width: 100%;
	}

	/* Ajustes para móvil */
	@media (max-width: 640px) {
		div {
			padding: 1rem;
		}
	}
</style>
